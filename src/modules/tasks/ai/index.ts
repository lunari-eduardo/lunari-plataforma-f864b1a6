/**
 * Superfície de IA do módulo Tasks.
 *
 * Importe SOMENTE deste arquivo no resto do app.
 *
 * - `tools` — catálogo de tools (commands/queries) expostas ao agente.
 * - `context` — snapshot da página para injetar no prompt.
 * - `permissions` — verificação declarativa de execução.
 */
export * from "./tools";
export * from "./context";
export * from "./permissions";
